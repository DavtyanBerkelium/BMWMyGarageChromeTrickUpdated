{
  "name": "bmw-mygarage-chrome-trick",
  "version": "2.1.0",
  "private": true,
  "description": "Exposes hidden production status, VIN, dates, and options on BMW MyGarage.",
  "scripts": {
    "test": "node --test 'tests/*.test.js'"
  }
}
